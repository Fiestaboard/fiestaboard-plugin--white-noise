{
  "id": "white_noise",
  "name": "White Noise",
  "version": "1.0.0",
  "description": "Gentle rain / white noise mode \u2014 softly cascading tiles that create a soothing ambient pitter-patter",
  "author": "FiestaBoard Team",
  "repository": "https://github.com/Fiestaboard/fiestaboard-plugin--white-noise",
  "documentation": "README.md",
  "icon": "cloud-rain",
  "category": "art",
  "settings_schema": {
    "type": "object",
    "properties": {
      "enabled": {
        "type": "boolean",
        "title": "Enable White Noise",
        "default": false
      },
      "intensity": {
        "type": "string",
        "title": "Rain Intensity",
        "description": "How many drops appear per frame. 'light' is a gentle drizzle, 'heavy' is a steady rain. Use 'custom' for manual control.",
        "enum": [
          "light",
          "medium",
          "heavy",
          "custom"
        ],
        "default": "light"
      },
      "drop_color": {
        "type": "string",
        "title": "Drop Color",
        "description": "Tile color for the raindrops.",
        "enum": [
          "white",
          "blue",
          "violet"
        ],
        "default": "white"
      },
      "drops_per_frame": {
        "type": "integer",
        "title": "Drops Per Frame",
        "description": "Number of new drops to spawn each frame (only used when intensity is 'custom'). Range: 1-22.",
        "minimum": 1,
        "maximum": 22,
        "default": 3
      },
      "max_drops": {
        "type": "integer",
        "title": "Max Simultaneous Drops",
        "description": "Maximum number of raindrops allowed on the board at once. Higher = denser rain. Range: 1-132.",
        "minimum": 1,
        "maximum": 132,
        "default": 30
      }
    }
  },
  "env_vars": [
    {
      "name": "WHITE_NOISE_ENABLED",
      "required": false,
      "description": "Enable the white noise rain effect",
      "default": "false"
    },
    {
      "name": "WHITE_NOISE_INTENSITY",
      "required": false,
      "description": "Rain intensity: light, medium, heavy, or custom",
      "default": "light"
    },
    {
      "name": "WHITE_NOISE_DROP_COLOR",
      "required": false,
      "description": "Raindrop tile color: white, blue, or violet",
      "default": "white"
    },
    {
      "name": "WHITE_NOISE_DROPS_PER_FRAME",
      "required": false,
      "description": "Number of drops per frame when using custom intensity (1-22)",
      "default": "3"
    },
    {
      "name": "WHITE_NOISE_MAX_DROPS",
      "required": false,
      "description": "Maximum simultaneous drops on board (1-132)",
      "default": "30"
    }
  ],
  "variables": {
    "simple": [
      "white_noise",
      "intensity",
      "drop_color",
      "active_drops",
      "drops_per_frame",
      "max_drops"
    ]
  },
  "max_lengths": {
    "white_noise": 200,
    "intensity": 6,
    "drop_color": 6,
    "active_drops": 3,
    "drops_per_frame": 2,
    "max_drops": 3
  },
  "fiestaboard_version": ">=2.10.0"
}
